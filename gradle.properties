org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=false
org.gradle.vfs.watch=false
org.gradle.configuration-cache=false

# Fabric toolchain shared by all version projects. 26.1+ uses the
# unobfuscated Loom plugin; older versions use the remapping plugin.
loom_version=1.17-SNAPSHOT
loader_version=0.19.3

# Mod coordinates
mod_version=1.1.0
maven_group=com.xiemu.crystallization
